# Backend URL — set in Vercel environment settings per environment
# Local:      http://localhost:8000
# Preview:    https://api.your-domain.com  (same backend for all previews)
# Production: https://api.your-domain.com
BACKEND_URL=http://localhost:8000
